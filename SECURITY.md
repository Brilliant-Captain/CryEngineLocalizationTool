# Security Policy

## 安全边界

本项目处理用户本地的 CryEngine PAK、GFX、DDS、字体和翻译表。工具不会上传这些文件，也不包含任何商业游戏资源、FFDec 二进制或用户机器路径。

安装功能会：

- 要求用户明确选择目标根目录和相对目标路径
- 检查配置的进程是否正在运行
- 为已有目标创建哈希验证的备份
- 使用临时文件和原子替换
- 写入可验证的 rollback record

Dry-run、CSV 导出、PAK 列表和提取不会修改游戏目录。

## 报告漏洞

请不要在公开 issue 中粘贴真实游戏资源、安装记录、用户路径、私钥或其他敏感数据。请通过 GitHub Security Advisories（如果仓库启用）或维护者公布的私密联系方式报告安全问题，并提供：

- 受影响的版本和平台
- 最小复现步骤（使用合成文件）
- 预期行为与实际行为
- 可能的安全影响

如果报告包含敏感文件，请先进行脱敏。维护者会确认收到报告，并在修复后发布说明。

## 用户安全建议

- 只运行来源可信的 EXE，并先核对 Release 提供的 SHA-256。
- 安装前先执行 Dry-run，并确认游戏已经退出。
- 不要把 FFDec、字体或商业游戏资源打包上传到 issue、PR 或仓库。
- 使用自定义语言 JSON 时，保持 key 和格式化占位符不变。
